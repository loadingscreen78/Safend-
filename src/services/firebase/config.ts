
// Frontend-only project - Firebase removed
// This file is kept for compatibility but Firebase is not initialized

export const db = null;
export const auth = null;
export const storage = null;

const app = null;
export default app;
